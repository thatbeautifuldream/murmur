{
  "name": "murmur",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "packageManager": "bun@1.3.10",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --parallel dev:web dev:desktop",
    "dev:web": "bun run --cwd apps/web dev",
    "dev:desktop": "VITE_DEV_SERVER_URL=http://localhost:5173 bun run --cwd apps/desktop dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "start": "bun run --cwd apps/desktop start",
    "package": "bun run speechd:build:release && turbo run build && bun run --cwd apps/desktop package",
    "package:unsigned": "bun run speechd:build:release && turbo run build && bun run --cwd apps/desktop package:unsigned",
    "speechd:build": "swift build --package-path native/speechd",
    "speechd:build:release": "swift build -c release --package-path native/speechd"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.6.0"
  }
}
